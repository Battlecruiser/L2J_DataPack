USER=root
PASS=
DBNAME=l2jdb
DBHOST=localhost
mysqldump --add-drop-table -h $DBHOST -u $USER --password=$PASS $DBNAME > l2jdb_backup.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < full_install.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/accounts.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/armor-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/boxaccess.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/boxes.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/castle.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/castle_door-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/castle_doorupgrade.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/castle_guards.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/castle_guards_skills.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/char_templates-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_friends.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_hennas.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_macroses.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_quests.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_recipebook.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_shortcuts.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/character_skills.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/characters.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/clan_data.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/clan_wars.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/class_list-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/droplist-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/etcitem-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/henna-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/henna_trees-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/items.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/locations-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/lvlupgain-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/mapregion-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/merchant_areas_list-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/merchant_buylists-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/merchant_lease.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/merchant_shopids-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/merchants-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/minions-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/npc-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/npcskills-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/pets.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/seven_signs.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/seven_signs_festival.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/siege_clans.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/skill_learn-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/skill_spellbooks.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/skill_trees-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/spawnlist-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/teleport-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/weapon-c3.sql
mysql -h $DBHOST -u $USER --password=$PASS -D $DBNAME < ../../sql/sql-experimental/zone-c3.sql